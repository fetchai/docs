import {
  Note,
  Properties,
  Property,
  Warn,
  Section,
  ApiIntro,
  Tag,
  CodeHeading,
} from "../../../components/mdx";
import { DropDownTabs, Tab } from "../../../components/mdx";
import { Row, Col } from "../../../components/mdx";
import {
  ApiEndpointRequestResponse,
  ApiRequest,
  ApiResponses,
} from "../../../components/api-endpoint";

# Spell Check Test Document

This document is intended to test the spell-checker script.

## Basic Text with Spelling Errors

The folowing paragraf contains spelling errrors. It shuld be flagged by the spell checker.

## Code Blocks

Here's a Python code block. Words like `def`, `return`, and `print` should **not** be flagged as spelling errors.

```python
def add_nubers(a, b):
    # Adds two numbers
    return a + b
```

# Example function call
result = add_numbers(3, 5)
print(result)