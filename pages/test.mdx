# Spell Check Test

This document is created to test the spell checker action.

## Basic Text

This is a simple text paragraph to test the spell checker. Check for **spelling** mistakes in this section.

### Code Blocks

Here’s a Python code block:

```python
def add_numbers(a, b):
    # Adds two numbers
    return a + b

# Example function call
result = add_numbers(3, 5)
print(result)
