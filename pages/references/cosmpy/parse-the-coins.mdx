# cosmpy.aerial.coins

Parse the coins.

### parse_coins

    ```py
    def parse_coins(value: str) -> List[Coin]
    ```

    **Arguments**:

        - `value:` coins.

    **Raises**:

        - `RuntimeError:` If unable to parse the value.

    **Returns**:

        Coins.
