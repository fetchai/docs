# cosmpy.aerial.urls

Parsing the URL.

## `Protocol` Objects

    ```py
    class Protocol(Enum)
    ```

    Protocol Enum.

    **Arguments**:

        - `Enum`: Enum.

## `ParsedUrl` Objects

    ```py
    @dataclass
    class ParsedUrl()
    ```

    Parse URL.

    **Returns**:

        Parsed URL.

### `rest_url`

    ```py
    @property
    def rest_url() -> Str
    ```

    Get the rest URL.

    **Returns**:

        rest urls.

### `parse_url`

    ```py
    def parse_url(url: str) -> ParsedUrl
    ```

    Initialize.

    **Arguments**:

        - `URL`: URL.

    **Raises**:

        - `RuntimeError`: If URL scheme is unsupported.

    **Returns**:

        Parsed URL.
